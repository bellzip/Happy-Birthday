// Placeholder for future interactivity, e.g., search functionality or order button actions.
document.querySelectorAll('.order-btn').forEach(button => {
    button.addEventListener('click', () => {
        alert("Order placed!");
    });
});
